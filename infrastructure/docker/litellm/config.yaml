# LiteLLM proxy: set OPENAI_API_KEY (or other provider keys) in the environment.
# See: https://docs.litellm.ai/docs/proxy/configs

model_list:
  - model_name: gpt-4o-mini
    litellm_params:
      model: openai/gpt-4o-mini
      api_key: os.environ/OPENAI_API_KEY
